version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "5000:5000"
    environment:
      - TELEGRAM_TOKEN=YOUR_BOT_TOKEN
      - WEBSITE_URL=http://localhost
